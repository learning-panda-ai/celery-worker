##################################################
# Celery Worker — Environment Variables         #
##################################################

# ── Redis ────────────────────────────────────────
# Must match the broker configured in the FastAPI service
REDIS_URL=redis://localhost:6379/0

# ── Milvus ───────────────────────────────────────
MILVUS_URI=http://localhost:19530
