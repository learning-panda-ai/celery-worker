##################################################
# Celery Worker — Environment Variables         #
##################################################

# ── Redis ─────────────────────────────────────
# Must match the REDIS_URL configured in the FastAPI backend
REDIS_URL=redis://localhost:6379/0

# ── Milvus ────────────────────────────────────
MILVUS_URI=http://localhost:19530
